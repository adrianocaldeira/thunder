@section Scripts
{
    <script type="text/javascript">
        $(function () {
            $.manager.userProfiles.index();
        });
    </script>
}
@Html.Partial("Message")
@using (Html.ContentBox("Perfis de Usuário", new ContentBoxHeaderButton { Title = "Novo Perfil", Text = "+", Url = Url.Action("New") }))
{
    using (Html.Grid(@Url.Action("Index"), new { id = "grid-profiles" }))
    {
    @Html.Gif("~/content/manager/images/grid_loading")
    }
}